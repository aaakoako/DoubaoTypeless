---
description: DoubaoTypeless V3 complete delivery, evidence and resumption rules
alwaysApply: true
---
# V3完整交付（流程补丁，不是新产品设计）

- 先读 `docs/v3-product/START_HERE.md` 与 `MASTER_PLAN.md`，再读当前 `docs/v3-delivery/CHECKPOINT.json`。旧 `docs/v3-delivery/` 只保留契约、检查器和证据，不再当产品入口。
- 最终目标是完整、好用、可发布的 Windows 候选：重构客户端和手机工作区，保住“插入并复制 → 收窗 → 手机新段落 → 上次可恢复”。不是只做V3-00、只修审计、只做手机或桌面入口。
- 只在已授权隔离工作区推进；不reset、不清除用户改动、不动日用数据；merge/tag/Release/覆盖安装需另行明确批准。
- 按 S0–S5 连续开发。一个切片实现、回归、记录、提交后继续下一可执行项，不为每卡索要“继续”。
- 输入只在活动中短暂显示HUD，平时无浮窗；主操作是插入并复制；电脑可改字；长文可读。HTML 参考图不是原生完成。
- 先图后文，不发Enter；身份不明拒绝远端插入；召回保留当前稿，UNKNOWN需恢复选择，不得伪造 CONFIRMED。
- 测试生产前端真实生成的请求，不给它偷偷补字段；不能拿模拟Target证明Cursor已通过。
- 当前报告 A01–A18 必须落实为真实修复，不只更新文档。缺设备仅阻塞相关验证；可安装依赖、UI、协议和CI由开发完成。
- 不改验收期望/严重性/冻结目录来换绿灯。原96条保留对应关系，体验变化按总计划覆盖，不能用旧规格否定已提出的修订。
- 源码/依赖/构建输入变化后，重新取得对应证据；候选结果绑定固定SHA和实际包哈希。
- 规则只是提示，CI和独立复核必须另外接入；格式检查通过不等于产品通过。
- 上下文/会话结束前更新CHECKPOINT：当前SHA、变更、失败项、实际证据、下一条具体开发动作。
