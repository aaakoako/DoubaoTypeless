# Grok 继续开发指令：复核 c3333a5

继续现有 DoubaoTypeless 完整 V3 产品，不另建工程、不重写产品范围。主规范仍为 `docs/v3-product/MASTER_PLAN.md`。本次复核补充放到 `docs/v3-product/reviews/c3333a5/`，不要覆盖上一轮原始证据。

先读取 `REVIEW.md`，核对当前HEAD与固定审查点：
- review HEAD：`c3333a55a516022397772eea932c8a21c7a26cd0`
- 产品源码点：`69eb4f875be0fea1ab12cce7b1e0d36f40d81b34`
- 两者只差CHECKPOINT。本次远端CI三个job成功，不再把旧libEGL失败当当前阻塞。

结论仍为REQUEST_CHANGES，但要保留真实进展：滚动HUD、页签、逐图队列、HTTP直接阻断修复、凭据失败内存回退、等待后的目标检查、区域截图传会话。不能一律回退这些代码。

不要继续把open_defects写成只有A18。按R01–R08把明确代码问题映射回原A项：

1. 完成回执：CONFIRMED不能发archived=null导致手机不清；图片UNKNOWN不能因为text为空而清附件；匹配draft/epoch/revision及图像版本，不只匹配文字。Alt+I等本地动作要真的把完成事件送到手机；四入口一致。
2. 重连：不要把旧缓存换成服务器的新epoch再自动sendDraft；握手先对账并保留双方。所有变更/插入消息绑定明确稿件身份。
3. 记住设备：电脑点击后，手机要得到并确认保存凭据；旧session失效必须转可信设备续接。撤销/关权限影响同device_id所有会话；忘记设备有真实动作。
4. 图片：服务端也要知道queued/editing/dirty，阻止Alt+I发原截图；完成且成品版本一致才准投递。再次编辑恢复源图像素与标注对象，不能只Node.create(scene)。持久化Blob/真实资产，不只blob URL；caption有实际投递语义。
5. AI：请求发起时冻结身份；采用/撤回验证版本，界面与数据一起更新；不要让旧建议覆盖新稿。正文/探测均异步并用有context的主线程调度，实际超时参数生效。
6. 目标/恢复：同包对应尝试和目标指纹，不能same_target=True写死；主目标附件观察不是永远UNKNOWN的占位实现。缺环境只阻塞实测，不免除这些代码。
7. 界面与候选：保留已经修好的HUD，但补阅读中不跳滚动、不自动隐藏；审阅的冲突/AI按钮按情境出现。最新源码重新打完整无控制台包，旧包哈希与新source_sha分开记录。

将本包的反例场景移入真实工程，直接导入修复后的生产模块，再加生产网页到真实服务的集成测试。不要改本包归档源码/期望制造通过；组件通过不等于真机通过。预期至少覆盖：
- 四入口A/B/C、失败纯图、同文新增附件、晚到回执；
- 离线旧稿与服务端新稿；
- 连上后才勾选记住、服务重启、同设备两会话后撤销；
- 原图编辑期间电脑快捷键、照片重开和刷新恢复；
- AI慢返回/新版本/新epoch/撤回后的真实编辑框；
- 最新完整包普通用户启动、正常退出及支持目标。

一个切片修复、测试、提交、记录后继续下个可执行项，不等用户逐卡说继续。会话预算不足保存具体CHECKPOINT；真实设备不足只列受影响验收，其他工作继续。交付目标仍是完整好用的V3，不是只修本报告后停止。

保持隔离分支和预览数据目录；不merge、不tag、不Release、不覆盖日用安装，不taskkill用户正在运行的预览。最终发布需用户针对源SHA、版本与完整包哈希另批。
