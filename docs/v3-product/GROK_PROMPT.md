# 发给 Grok 的完整任务指令

你接管 aaakoako/DoubaoTypeless 的完整 V3 产品交付。
阅读 docs/v3-product/START_HERE.md 和 MASTER_PLAN.md，并打开同目录 UI_BOARD.html 理解界面，核对 COVERAGE.json。审查基线 4eb8c4454a879e6b7ce11cf47e1bb0bfdba93b9a，旧版能力基线 e6b5b085d055d6f4306d486fb6d8e3cd5dfa84d5。先检查真实HEAD/分支/工作区；新提交先比较，不回退覆盖，不从头重写新工程。

目标：重构后的 Windows 图形客户端＋手机图文工作区，保留旧版好体验，最终交付好用、可发布的完整V3候选。不是只修审计问题，不是只做手机，不是只补桌面入口，也不是把工程预览不断交回来。

本入口替代旧“只做V3-00”“每卡后停止”“桌面只是投递器”“永远两行”“UNKNOWN只能一直占着手机旧稿”等冲突指令；安全、图像顺序、真实证据、隔离与发布审批仍有效。原96条保留映射，本次明确的体验修改按MASTER_PLAN执行，不用旧字面要求否定用户反馈。

必须保留并接通：
1. 插入并复制（主按钮/Alt+I）、独立只复制、电脑改字和正常编辑快捷键；复制不清稿。
2. 长文有上限自动增高，主动展开全文；空闲无HUD，插入后收窗；手机新一段与上次恢复并存，晚回执不删新稿。
3. 正常Windows客户端、扫码零手输、本机权限批准、设置、最近内容、词库、模型预设、诊断、托盘、自启动、重复启动唤起和真实退出。
4. 手机原输入法、截图/相册/裁剪/标注/轻量白板、重新编辑和撤销；每图状态完整，失败不能静默漏图。
5. 已处理图片先文字后进入真实主目标，不发送Enter。当前稿与上次稿分离；同类不同目标也要停止；未知/部分结果有真实恢复选择。
6. BYOK是手动文字术语辅助，配置的模型真实生效，异步不堵UI，结果可接受/拒绝/撤回；无Key核心完整可用。
7. 平面统一的Qt/手机界面，无导航按钮立体阴影；图标/托盘图同轮完成，不拿HTML示意当客户端截图。

按MASTER_PLAN S0–S5连续开发：先修已确认缺陷/测试基础，恢复高频文字体验，再闭合图文和完整编辑、恢复/设置/视觉，最后最终包回归。每个切片写能暴露问题的测试、修正式代码、从正式入口运行、提交隔离分支并记录，然后继续下一可执行工作，不要求用户逐卡说“继续”。

不要只更新CHECKPOINT、已知限制或审计脚本。不要因为有116个单测、一个EXE或独立审阅等待，就说只剩用户核查。本次复核发现的A01–A18要落实到实际修复；更早已修问题不要重新破坏。最新云端CI是5 failed/108 passed/3 skipped，词库一项为LF/CRLF差异，不是已证明日用内容被改。

自己完成可执行环境准备、真实GUI点击、退出/重启、后端和正式前端测试、包构建。缺ADB不等于不能手动手机验收；缺comtypes等开发依赖先处理，不写成必须用户解决的设备问题。真正缺设备/系统授权时集中给最小步骤，继续不受阻工作。发现必须改变主目标或新增付费服务时单独说明证据和方案，不自行砍需求。

不得修改原测试期望造绿，不将模拟PasteTarget/HTML原型冒充真实Cursor或聊天网页。图片、文字、窗口、剪贴板、新稿分别检查实际结果。原始证据只读保留，更新结果绑定被测源SHA与完整包清单。不是重新反复运行审计包中的旧源文件。

交付报告先说用户已能做什么、旧体验恢复什么、还缺什么，再列测试/源码/制品。会话或额度不足时保存CHECKPOINT：当前SHA、未提交变更、下一具体动作、失败项、环境缺口，下一轮可以一句“继续V3”接着做；不得承诺会话结束后后台继续。

只在隔离分支与预览数据目录操作，不merge、不打tag、不发布Release、不覆盖日用config/词库/历史/注册表。可以并应该做到完整RELEASE_READY候选；公开发布最后由用户针对具体SHA、版本、完整包hash批准。最终候选没有达到计划要求时如实列差距并继续开发，而不是把未完成工作换个名字交差。

现在从真实仓库接管并开始执行，不再先写一轮泛泛计划。

---

## 下一会话只需发

继续V3。读取 docs/v3-product/START_HERE.md、MASTER_PLAN.md 和现有CHECKPOINT；核对真实HEAD后从记录的下一开发动作继续，保持完整产品终点与隔离/发布边界。
