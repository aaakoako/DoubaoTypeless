# V3-03 · 不可变图文与投递状态合同

**状态：NOT_STARTED；本文件不是完成报告。**

前置依赖：V3-00。先读：`main.py::_pending_*、_do_insert；bridge.py::notify_cleared`。

规格：`spec/04_ARCHITECTURE_AND_PROTOCOL.md`；实现建议落点：`src/doubao_typeless/core/、contracts/、tests/test_bundle.py`。新目录是建议，不代表已经存在；现有目录不同时先交路径映射，勿改验收含义。

## 执行步骤与可观察结果

1. **快照冻结**：提交稿r3后将当前稿改成r4并修改图片顺序。完成后必须观察：Attempt仍只读取r3图文；r4未被污染。验收层级：自动，对应 `AC3-013`。
2. **同版冲突**：发送同draft+epoch+revision但不同hash的两份内容。完成后必须观察：拒绝冲突并要求重同步，不覆盖已ACK版本。验收层级：自动，对应 `AC3-014`。
3. **精确保留文本**：输入前导空格、尾换行、emoji与混合中英文。完成后必须观察：序列化往返逐字一致，不trim/NFKC。验收层级：自动，对应 `AC3-015`。
4. **清空版本隔离**：投递r3后手机已继续到r4，再送r3归档回执。完成后必须观察：r4保留；只归档匹配r3，其他设备不清空。验收层级：自动，对应 `AC3-016`。

## 开发顺序

先加能够暴露本卡风险的测试/复现脚本，再做最小实现；接入到真实服务或平台适配；检查禁用/空/失败路径；最后比对交互和性能。不要只实现正常演示状态。

## 完成条件

本卡所有用例有固定提交下的实际证据；自动测试记录命令和退出码；原生/真机项有环境、操作录像或连续截图和结果。缺少环境写BLOCKED_NATIVE，不写PASS。前置卡未通过的能力不能被本卡假定已存在。集成后相关回归用例必须重新跑。

## 本卡禁止

不删除现有日用数据，不改稳定分支/Release，不临时硬编码坐标/Key/虚假回执，不以sleep替代可观察结果，不引入规格外功能。不要让手机网络消息直接变成任意键鼠脚本；不要发Enter。

## 交付回复模板

实现范围；修改文件；实际命令/退出码；通过/失败/未执行用例ID；实机证据路径；与原型差异；尚未解决项；提交SHA。建议每卡独立提交，但不得未经授权推送或合并。
