# V3-09 · 手机单页Composer

**状态：NOT_STARTED；本文件不是完成报告。**

前置依赖：V3-02, V3-06。先读：`phone.html、旧config默认行为`。

规格：`spec/01_UX_AND_VISUAL.md`；实现建议落点：`web/src/ui/composer.ts、web/src/composer/draft.ts、web/src/styles/`。新目录是建议，不代表已经存在；现有目录不同时先交路径映射，勿改验收含义。

## 执行步骤与可观察结果

1. **纯文字零负担**：新用户不配Key只语音输入并按电脑热键。完成后必须观察：无必须打包/发送页面，无空附件占位栏。验收层级：真机，对应 `AC3-037`。
2. **图序与删除**：加入3图，拖动排序、左右移动、删除并撤销。完成后必须观察：预览与投递顺序一致；撤销恢复原图序。验收层级：自动+真机，对应 `AC3-038`。
3. **手机按钮语义**：空内容、失联、上传中、已同步分别点主按钮。完成后必须观察：禁用/准备/请求行为符合规范，不离线排队按键。验收层级：自动，对应 `AC3-039`。
4. **适配窄屏**：360/390/430px宽以及键盘打开测试。完成后必须观察：44px命中区域、主按钮不被遮挡、无整页水平溢出。验收层级：真机，对应 `AC3-040`。

## 开发顺序

先加能够暴露本卡风险的测试/复现脚本，再做最小实现；接入到真实服务或平台适配；检查禁用/空/失败路径；最后比对交互和性能。不要只实现正常演示状态。

## 完成条件

本卡所有用例有固定提交下的实际证据；自动测试记录命令和退出码；原生/真机项有环境、操作录像或连续截图和结果。缺少环境写BLOCKED_NATIVE，不写PASS。前置卡未通过的能力不能被本卡假定已存在。集成后相关回归用例必须重新跑。

## 本卡禁止

不删除现有日用数据，不改稳定分支/Release，不临时硬编码坐标/Key/虚假回执，不以sleep替代可观察结果，不引入规格外功能。不要让手机网络消息直接变成任意键鼠脚本；不要发Enter。

## 交付回复模板

实现范围；修改文件；实际命令/退出码；通过/失败/未执行用例ID；实机证据路径；与原型差异；尚未解决项；提交SHA。建议每卡独立提交，但不得未经授权推送或合并。
