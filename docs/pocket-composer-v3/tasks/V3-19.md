# V3-19 · 引导、设置与快捷键迁移

**状态：NOT_STARTED；本文件不是完成报告。**

前置依赖：V3-07, V3-09, V3-10, V3-17, V3-18。先读：`config.py、gui.py、hotkeys.py、旧AGENTS.md`。

规格：`spec/01_UX_AND_VISUAL.md`；实现建议落点：`ui/pairing.py、ui/settings.py、web/src/ui/onboarding.ts、storage/migration.py`。新目录是建议，不代表已经存在；现有目录不同时先交路径映射，勿改验收含义。

额外必读：`spec/10_COMPONENT_DETAILS.md` 的C/F/G/H，落实所有权限、恢复和设置状态。

## 执行步骤与可观察结果

1. **三步上手**：新用户扫码→输入测试→练习插入，不配API。完成后必须观察：引导后空闲无窗口，知道召回快捷键。验收层级：Windows+真机，对应 `AC3-077`。
2. **热键迁移**：导入自定义旧快捷键，旧局部Alt+Shift+I跳过纠错。完成后必须观察：保留自定义；清楚说明新召回语义，不同时绑定两种动作。验收层级：Windows，对应 `AC3-078`。
3. **真冲突反馈**：外部占用建议热键，再注册候选键位。完成后必须观察：失败提示改键，不把语法合法当注册成功。验收层级：Windows，对应 `AC3-079`。
4. **权限不诱导**：拒绝截图或远程插入，仍使用手机文字同步。完成后必须观察：功能边界准确、可改授权，不要求全权限才能用。验收层级：Windows+真机，对应 `AC3-080`。

## 开发顺序

先加能够暴露本卡风险的测试/复现脚本，再做最小实现；接入到真实服务或平台适配；检查禁用/空/失败路径；最后比对交互和性能。不要只实现正常演示状态。

## 完成条件

本卡所有用例有固定提交下的实际证据；自动测试记录命令和退出码；原生/真机项有环境、操作录像或连续截图和结果。缺少环境写BLOCKED_NATIVE，不写PASS。前置卡未通过的能力不能被本卡假定已存在。集成后相关回归用例必须重新跑。

## 本卡禁止

不删除现有日用数据，不改稳定分支/Release，不临时硬编码坐标/Key/虚假回执，不以sleep替代可观察结果，不引入规格外功能。不要让手机网络消息直接变成任意键鼠脚本；不要发Enter。

## 交付回复模板

实现范围；修改文件；实际命令/退出码；通过/失败/未执行用例ID；实机证据路径；与原型差异；尚未解决项；提交SHA。建议每卡独立提交，但不得未经授权推送或合并。
