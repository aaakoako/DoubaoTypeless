# 蓝紫整合体验包（2026-09-22）

用户确认此前绿色预览来源错误，授权以此前交付的蓝紫版为底整合有效修复并提供体验包。本分支基于 b32d2d536937d0fcad2c0e2ea76742d60fa47c42（产品 aec0ee81bcd3274000a8799df54ef57cadfab8b3），不是绿色 progress-fixes 的整体合并。

保留手机主稿、IndexedDB 事务与离线恢复、电脑改字、多图按顺序后贴正文、图片明确保存/取消、独立授权确认发送、UIA 辅助进程和受限 Composer 定位。图片/历史持久化及运行诊断在此基线更完整，不复制旧实现。

移植与补充：
- 浮窗真实透明圆角、独立卡片边框；维持 Mist Indigo 主题及新图标。
- 正文追尾、人工滚动位置保留；拖动/选中时暂停文档更新，“回到最新”恢复；空稿进入新轮次清理旧选择。
- 修复按钮栏透明样式覆盖主按钮背景和关闭按钮文字裁切。
- BYOK 使用标准 messages 请求，检测与正文使用相同参数，内部 TypeError 不重复发请求，明确400/403提示。
- 使用现有 FocusSnapshot 绑定投递起点，禁止校验后换输入控件；不退回旧UIA实现。
- 手机插入防重入，延迟回执关联当前 intent，旧 nonce 不跨重连发出；保留同稿 durable intent。
- 打包排除PATH中不兼容的ICU DLL；增加独立Windows桌面的冻结启动与确认退出检查。

验证以本次实际报告为准。前次 aec0ee8 的 EXE/浏览器报告是历史证据，不能冒充本包验证。手机页面自动化含显式网络替身；Qt离屏像素检查不等于用户桌面验收；真实Codex/Cursor、Android/iOS仍需本包体验。

本包不merge主干、不打tag、不发布Release、不覆盖日用数据。候选与证据位于 G:/AgentStorage/Deliveries/typeless-integrated-20260922。
