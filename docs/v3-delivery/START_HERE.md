# V3 完整交付：历史执行入口

> **当前唯一产品/执行规范是 [`docs/v3-product/START_HERE.md`](../v3-product/START_HERE.md) 与 [`MASTER_PLAN.md`](../v3-product/MASTER_PLAN.md)。** 本目录只保留执行契约、检查器和已有证据，不再作为开发入口。不要用本页的旧“只做V3-00 / 每卡后停止”措辞覆盖总计划。

**这是执行流程修订，不是 V4 产品设计。** 沿用既有 Pocket Composer v3 的页面、交互、R01–R10、24 张任务卡与 96 项验收。它不把白板、BYOK、召回或设置删出范围。

## 给 ako

把本目录放到工作仓库的 `docs/v3-delivery/`，交给开发 Agent。无需把原设计文档重新发一遍、也不需要你逐卡选择任务。先让 Agent 合并规则与已有规则，禁止无检查覆盖已有文件。

本包没有接入你的仓库，没有修改应用，没有运行你电脑上的任何操作。新检查器只做证据一致性筛查，不能替代生产测试和独立复核。

## 交给开发 Agent 的指令

```text
读取 docs/v3-delivery/EXECUTION_CONTRACT.md。
本次目标是完成整个既定V3，得到可发布的Windows候选；不是只执行V3-00或只修审计问题。
本执行入口替代旧 spec/08_AI_HANDOFF.md 的“只执行一张卡并停止”及旧返修指令的“只修R0+R1后停止”。
原产品范围、原验收含义、安全和发布授权边界继续有效；不得利用本入口绕过现有权限限制。

先检查当前完整HEAD/工作区/已有执行记录，与审查基线9dd3a369f1b566d0fdc548cee3af934523e9b8db比较。
那是审计基线，不是命令你checkout/reset回该提交。保留后续用户改动和已验证实现。

在已授权隔离环境，按S0→S5完成可实测切片；每次只聚焦一个切片，实际回归、记录后继续下一项。
不要因一张卡结束索要“是否继续”。跨上下文前保存CHECKPOINT，下次从断点继续。
先修审计发现、授权隔离、CI和真实前端图文链路；同时尽早验证目标Cursor可观察能力。
有代码缺陷写FAIL；未验证写NOT_RUN；真实缺环境写BLOCKED_ENV或BLOCKED_NATIVE并说明最小解除动作。
缺环境不能替代没写完的代码，也不能让全部不相关工作停住；不得把有原生依赖的功能宣称完成。

原96项验收全部保留。源码文件存在、样板target通过、原型通过都不是产品PASS。
真实浏览器必须执行生产前端请求，不得在测试驱动额外补生产端没有发出的字段。
有依赖或构建输入变更时重新验证；最终候选绑定完整SHA和包哈希。

可持续开发/测试/构建隔离候选；不得自动merge/tag/Release、覆盖稳定安装、扩大权限或真实消费模型额度。
实际需要手机操作/系统授权/独立评审时，给出最小动作和预期证据，保存断点，而不是把剩余工作推给用户猜。
完整范围+实际产品验收+独立复核通过后，交付RELEASE_READY候选与发布审批单。
公开发布仍等待ako针对具体SHA、包哈希和版本号明确批准。
```

恢复会话时只需一句：`按 docs/v3-delivery/CHECKPOINT.json 继续完成V3到可发布候选，先核对当前HEAD，不重新设计、不重复已证实工作。`

## 首次接管动作

1. 读执行契约、当前复核报告和V3原规格索引；登记源码/配置/测试/构建入口实际路径。
2. 把 `rules/v3-delivery.mdc` 合并到仓库 `.cursor/rules/v3-delivery.mdc`，先比较已有规则，不覆盖其他项目规则。检查当前Cursor里规则实际生效；这些提示不能取代权限控制。
3. 基于 `templates/checkpoint.example.json` 创建真实 CHECKPOINT；示例不是事实报告。
4. 先完成S0，建立能拒绝失败的生产CI，再继续S1。不要只安装本检查器就宣称S0结束。

详细流程：`EXECUTION_CONTRACT.md`。检查器用法：`tools/README.md`。
