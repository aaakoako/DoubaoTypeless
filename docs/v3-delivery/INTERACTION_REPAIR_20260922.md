# 实际使用反馈返修（2026-09-22）

产品源码固定为 `d5f978b66fc21f10aa3d601b942d1e153c35db90`，本文件为后续交付记录，不将文档 HEAD 冒充产品构建来源。

## 行为改变

- 浮窗顶部可拖动并保存位置。未完成内容不再因空闲或切换应用自动隐藏；原置顶、不抢键盘焦点的窗口行为保留。设置与浮窗并存。
- 展开编辑增加“返回浮窗”；关闭展开窗口也返回并保留编辑。复制取当前可见编辑稿，原手机稿仍保留。
- 显式电脑插入冻结已收到稿，不再等待手机在线准备；插入前后仍校验目标和来源。失败时仅在无已执行步骤且错误类型允许时复制退路，不假报成功、不清稿。
- 定位候选和图文恢复不再因普通插入自动打开阻挡对话框；用户主动定位/恢复时才展示。
- 白板文字从原生 prompt 改为页面内编辑，文字工具不捕获后续按钮指针；取消、保存、切换工具可结束编辑。工具有本地 SVG 和文字标签。
- 手机浏览器离线事件立即更新状态，重连恢复同步；PC 离线稿成功回执持久化。未改旧稿在重连后进入下一轮，手机新增稿不被旧回执清空。
- 连接页保留二维码，并在消费/过期后刷新；短码作为备用。区分有配对凭据与实际手机在线。

具体用户状态、操作和预期结果已写入 MASTER_PLAN 末节，明确替换过去与本次需求冲突的行为约定。

## 验证与独立复核

当前全量 Windows 回归 **495 passed**（47.08s）；TypeScript、生产构建和 diff 空白检查通过。
`test_v3_user_flow_repair.py` 覆盖实际 Qt 设置共存/拖动保存/返回、二维码再次配对、生产网页触控文字退出及编辑取消、真实桥接离线/重连快照清理、电脑改字后的复制。
浏览器使用实际生产资源与桥接；原生投递使用明确替身，截图像素源为合成图，不据此声称验证真实桌面焦点或截图。

独立只读审查定位原来的手机在线依赖、配对后隐藏二维码、设置互斥、自动恢复弹窗，并发现本轮中电脑改稿 B 后复制仍取 A；已修复并增加回归。此审查不是独立 GUI 验收。

冻结 EXE 使用单独 Windows 桌面、离屏 Qt、无效热键及独立 IPC/数据目录：HTTP 就绪、退出确认、正常退出日志、退出码 0、无强杀。没有触碰正在使用的旧包或用户输入。
截图来自最终生产页面和实际 Qt 控件；截图辅助进程退出时有一次 WinError 10054 socket reset，进程退出 0，未隐去该限制。

**尚未验证：真实 Codex/Cursor 的版本差异、焦点及图片附件，实体手机输入法/软键盘，真实多屏拖动与长期使用。** 该包为返修体验包，尚未取得用户体验验收。

## 可打开的制品

交付目录：`G:\AgentStorage\Deliveries\typeless-interaction-repair-20260922`。
入口：`DoubaoTypelessV3PreviewUI/启动交互返修版.cmd`；操作结果和截图见 `体验说明.md`。
新包使用自身 `preview-data`，不覆盖旧包；提示用户退出旧预览后再启动，按新连接页扫码，不沿用旧 8767 地址。

ZIP：`DoubaoTypeless_Interaction_d5f978b.zip`，81229295 bytes。
SHA-256：`1d2c30f5b71c5bd028083f3dfc6bae5f4e1c66098ac59f156a985816a7de8278`。
完整 943 文件集、长度和 SHA-256 已逐项对比 ZIP；保留 `manifest.json`、`package.json`、`pytest-final.txt`、`build.log`、`frozen-smoke.json` 和截图。

未合并主干、未打 tag、未发布 Release。旧包及其运行数据保留原处。
